Act as a senior React/Vite/Capacitor mobile engineer.

We need to fix two production-critical issues in the Quran app before creating a new TestFlight build.

Important:
- Do not break the current web app.
- Do not break Capacitor iOS/Android setup.
- Do not change the database unless absolutely required.
- Keep Arabic RTL layout.
- Keep existing Quran, prayer times, adhkar, settings, and notification logic.
- The iOS TestFlight app is built from bundled Capacitor assets, so after fixes we must run build:mobile and cap sync.

ISSUE 1: Muezzin preview audio plays the wrong sound
Current behavior:
- In the browser, audio now plays, but it plays "بسم الله الرحمن الرحيم" or Quran recitation audio.
- This is wrong. The muezzin preview must play an actual adhan preview, not Quran recitation, basmalah, or placeholder audio.

Required:
1. Inspect all muezzin audio mappings.
2. Find where the current audio file "بسم الله الرحمن الرحيم" is coming from.
3. Remove this incorrect placeholder from all muezzin preview mappings.
4. Each muezzin must have a valid adhan MP3 file only.
5. Store local adhan files under:
   public/audio/adhan/
6. Use clear filenames, for example:
   public/audio/adhan/yasser-al-dosari.mp3
   public/audio/adhan/ali-ahmad-mulla.mp3
   public/audio/adhan/abdulbasit-abdul-samad.mp3
7. Update the muezzin data so each item uses the correct adhan file path:
   /audio/adhan/file-name.mp3
8. If real adhan MP3 files are not currently available in the repo:
   - Do not use Quran recitation or basmalah as fallback.
   - Keep the preview disabled for that muezzin.
   - Show:
     "ملف الأذان غير متوفر حالياً"
   - Provide an exact list of required MP3 filenames that I need to upload.
9. Do not show false success if the audio is not an actual adhan file.

Expected audio behavior:
- User taps preview.
- Actual adhan audio plays.
- Tapping another muezzin stops the previous audio.
- No Quran recitation or basmalah is used as muezzin preview.

ISSUE 2: Quran/Mushaf works in browser but fails in TestFlight
Current behavior:
- Quran/Mushaf images or ayahs appear in the browser.
- In TestFlight, Mushaf/Quran does not load.
- It shows an error similar to:
  "تعذر تحميل الآيات، تحقق من اتصالك بالإنترنت"

Required:
1. Inspect how Quran/Mushaf data and images are loaded.
2. Identify why it works in browser but fails inside Capacitor iOS/TestFlight.
3. Check for all possible causes:
   - wrong asset paths
   - relying on Replit-only paths
   - files not copied into dist/mobile
   - files not copied into ios/App/App/public
   - API blocked inside iOS WebView
   - CORS issue
   - use of relative paths instead of absolute public paths
   - missing local JSON/images in mobile build
4. Fix the mobile build so Quran/Mushaf works inside Capacitor iOS.
5. Prefer local bundled assets for Quran core data/images if possible.
6. If using remote API, add fallback and better error handling.
7. Do not show "تحقق من اتصالك بالإنترنت" unless the failure is truly network-related.
8. Use better Arabic errors:
   - Missing local files:
     "ملفات القرآن غير متوفرة حالياً"
   - API failure:
     "تعذر الاتصال بمصدر بيانات القرآن"
   - Parse failure:
     "تعذر قراءة بيانات القرآن"
   - Unknown:
     "حدث خطأ أثناء تحميل الآيات"

Asset verification:
After the fix, verify that required Quran/Mushaf files exist in:
- dist/mobile/
- ios/App/App/public/
- android/app/src/main/assets/public/

Build verification:
Run from repository root:
pnpm install
cd artifacts/quran
pnpm build:mobile
npx cap sync ios
npx cap sync android

Then confirm:
1. Browser still works.
2. Mobile build succeeds.
3. Quran/Mushaf files are included in iOS public assets.
4. Adhan MP3 files are included in iOS public assets.
5. No incorrect basmalah/recitation audio is used as adhan preview.
6. The app is ready for a new Codemagic iOS TestFlight build.

Important final note:
Republishing Replit does not update the TestFlight app if the iOS app uses bundled Capacitor assets. After these fixes, I must push to GitHub and run a new Codemagic build to upload a new version to TestFlight.

After completion, provide:
- Root cause of wrong muezzin audio
- Root cause of TestFlight Quran/Mushaf failure
- Files changed
- Exact audio files required or added
- Exact Quran/Mushaf assets included
- Build/test results
- Whether I need to run Codemagic again